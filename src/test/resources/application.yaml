logging:
  config: classpath:common-logback-test.xml
  level:
    ROOT: INFO
    com.company.project: DEBUG

spring:
  application:
    name: ms-template-project

application:
  client:
    notification:
      url: http://localhost:8081